{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "noImplicitAny": true, // 값으로 지정하면 암시적 any에 대해서 허용하기
    "strictNullChecks": false, // null & undefine의 허용여부
    "noEmitOnError": false // 오류가 있을때 컴파일을 할지 말지 선택함.
  }
}
